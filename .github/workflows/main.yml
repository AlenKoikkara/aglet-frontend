name: Deploy Frontend to EC2

on:
  push:
    branches:
      - main  # Change to "dev" for the dev branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Create .env.production File
        run: |
          echo "REACT_APP_BACKEND_BASE_URL=${{ secrets.REACT_APP_BACKEND_BASE_URL }}" >> .env.production
          echo "CI=false" >> .env.production

      - name: Fetch EC2 host key
        run: |
          ssh-keyscan -H 44.203.204.86 >> ~/.ssh/known_hosts
          
      - name: Set up SSH key
        run: |
          echo "${{ secrets.EC2_SSH_PRIVATE_KEY }}" > /tmp/ec2_key.pem
          chmod 600 /tmp/ec2_key.pem

      - name: Build Project
        run: npm run build:prod

      - name: Deploy to EC2
        env:
          PRIVATE_KEY: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          HOST: ${{ secrets.EC2_HOST }}
          USER: ${{ secrets.EC2_USER }}
        run: |
          # Copy the build folder to EC2 instance
          scp -i /tmp/ec2_key.pem -r build/* ec2-user@44.203.204.86:/var/www/frontend-prod/

          # Restart Nginx (or any other server you use)
          ssh -i $PRIVATE_KEY $EC2_USER@$EC2_HOST 'sudo systemctl restart nginx'
